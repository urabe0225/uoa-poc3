- name: register cygnus-mongo subscriber
  uri:
    url: http://localhost:8080/orion/v2/subscriptions/
    method: POST
    body_format: json
    status_code: 201
    headers:
      Authorization: "bearer {{ tokens[0] }}"
      Fiware-Service: "{{ fiware.service }}"
      Fiware-ServicePath: "{{ fiware.delivery_robot.service_path }}"
    body: "{{ lookup('template','cygnus_mongo_subscriber1.json.j2') }}"

- uri:
    url: http://localhost:8080/orion/v2/subscriptions/
    method: POST
    body_format: json
    status_code: 201
    headers:
      Authorization: "bearer {{ tokens[0] }}"
      Fiware-Service: "{{ fiware.service }}"
      Fiware-ServicePath: "{{ fiware.delivery_robot.service_path }}"
    body: "{{ lookup('template','cygnus_mongo_subscriber2.json.j2') }}"

- uri:
    url: http://localhost:8080/orion/v2/subscriptions/
    method: POST
    body_format: json
    status_code: 201
    headers:
      Authorization: "bearer {{ tokens[0] }}"
      Fiware-Service: "{{ fiware.service }}"
      Fiware-ServicePath: "{{ fiware.delivery_robot.service_path }}"
    body: "{{ lookup('template','cygnus_mongo_subscriber3.json.j2') }}"

- uri:
    url: http://localhost:8080/orion/v2/subscriptions/
    method: POST
    body_format: json
    status_code: 201
    headers:
      Authorization: "bearer {{ tokens[0] }}"
      Fiware-Service: "{{ fiware.service }}"
      Fiware-ServicePath: "{{ fiware.delivery_robot.service_path }}"
    body: "{{ lookup('template','cygnus_mongo_subscriber4.json.j2') }}"

- uri:
    url: http://localhost:8080/orion/v2/subscriptions/
    method: POST
    body_format: json
    status_code: 201
    headers:
      Authorization: "bearer {{ tokens[0] }}"
      Fiware-Service: "{{ fiware.service }}"
      Fiware-ServicePath: "{{ fiware.delivery_robot.service_path }}"
    body: "{{ lookup('template','cygnus_mongo_subscriber5.json.j2') }}"

- uri:
    url: http://localhost:8080/orion/v2/subscriptions/
    method: POST
    body_format: json
    status_code: 201
    headers:
      Authorization: "bearer {{ tokens[0] }}"
      Fiware-Service: "{{ fiware.service }}"
      Fiware-ServicePath: "{{ fiware.delivery_robot.service_path }}"
    body: "{{ lookup('template','cygnus_mongo_subscriber6.json.j2') }}"
