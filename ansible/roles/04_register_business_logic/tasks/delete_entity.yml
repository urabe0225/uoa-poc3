- name: delete entity
  uri:
    url: "http://localhost:8080/orion/v2/entities/{{ entity.id }}"
    method: DELETE
    status_code: 204
    headers:
      Authorization: "bearer {{ tokens[0] }}"
      Fiware-Service: "{{ fiware.service }}"
      Fiware-ServicePath: "{{ _service_path }}"
